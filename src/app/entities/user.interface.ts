export interface User {
    userid ? : string;

    username ? : string;
    password ? : string;
    firstname ? : string;
    lastname ? : string;
    email? : string;
    phone ? : string;
    enabled ? : string;
    source ? : string;
    socialid ? : string;
    profileimgurl ? : string;
    gender ? : string;
    dob ? : string;
    startdate ? : string;
    enddate ? : string;
    status ? : string;
    centerid ? : string;
    address ? : string;
   
  } 